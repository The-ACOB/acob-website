import TeamClient from './TeamClient';

export const metadata = {
  title: 'Join Our Team | ACOB - Careers & Opportunities',
  description: 'Explore career opportunities at Applied Cognitio Olympiad Bangladesh (ACOB). Apply for open roles or submit a general application.',
};

export default function TeamPage() {
  return <TeamClient />;
}